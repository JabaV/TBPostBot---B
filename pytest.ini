[pytest]
# Ключевой флаг: импорт модулей без исполнения верхнеуровневого кода (__main__)
addopts = -q --doctest-glob="*.py" --doctest-modules --import-mode=importlib
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .